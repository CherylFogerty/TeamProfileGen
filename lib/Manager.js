class Manager {
    constructor (officeNumber){
        this.officeNumber = officeNumber;
      }
}

getRole() 
    let name = name ();
    return name.getName() - this.name;


class Manager extends Employee {
    constructor(name, id, email, officeNumber) {
        super(officeNumber);
    }
}